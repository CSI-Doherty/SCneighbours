% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visulisation.R
\name{visualize_neighbourhood}
\alias{visualize_neighbourhood}
\title{visualize_neighbourhood}
\usage{
visualize_neighbourhood(
  obj,
  meta_data_column,
  meta_data_highlight,
  reduction = NULL,
  density = F,
  graph = "RNA_nn",
  percent = 95
)
}
\arguments{
\item{obj}{A Seurat, SingleCellExperiment or SCNeighbours object containing single-cell data.
If in Seurat or SingleCellExperiment form will firist be converted to SCneighbours format}

\item{meta_data_column}{Name of the metadata column in the object to pull values from
for identifying the cells of interest.}

\item{meta_data_highlight}{The specific value within meta_data_column to
analyze neighbours for (e.g., a specific cluster or cell type).}

\item{reduction}{Name of the dimensionality reduction to plot
(e.g., "umap", "tsne", "pca").}

\item{density}{Logical indicating whether to plot density contours (TRUE) or
simple point highlights (FALSE). Default is FALSE.}

\item{graph}{either a nearest neigbour graph in igraph, dgCMatrix or Seurat format, or the name of a graph stored in the Seurat object.
(e.g., "RNA_nn", "RNA_snn", or "SCT_nn").}

\item{percent}{Percentile level for density contours when density=TRUE
(default: 95). Only used when density=TRUE.}
}
\value{
A ggplot2 object showing the dimensionality reduction with neighbours
  highlighted either as red points (density=FALSE) or as density contours
  (density=TRUE).
}
\description{
Visualizes the neighbours of cells belonging to a specific group
  on a dimensionality reduction plot (e.g., UMAP, t-SNE, PCA). Can display
  neighbours either as simple highlighted points or as density contours showing
  the spatial distribution patterns. This helps understand how cell types
  interact spatially and identify neighbourhood structures.
}
\details{
Visualize Cell Neighbourhoods on Dimensionality Reduction
}
